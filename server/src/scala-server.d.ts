// The scala-server.js file is only generated into the output folder
// so there is no source file and this typings file stands alone
// in the src directory.

import { AuthorResolvers } from "./generated/graphql"

export type Authors = {
  resolvers: AuthorResolvers
}
